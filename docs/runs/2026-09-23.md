# Runs of 2026-09-23

Same rig as [2026-09-21.md](2026-09-21.md). Evidence under `.build/` (ignored), minified to text.

| Time | Run | Result | Evidence folder | What it showed |
| --- | --- | --- | --- | --- |
| 21:08 | halo only, against the `Maintain()` fix (`b8d7f25`), 3 scenarios | `failed`: 2 passed, 1 failed | `.build/pickle-run-2026-09-23-2108-halo/` (summary retyped, no junit: the shared folder was overwritten before the copy) | Normal speed and 3x: alive on 300 of 300 ticks, ping age 0 on every one. **The fix works there.** Ultrafast: alive on 186 of 300 samples |
| 21:21 | full suite, pass A English, 25 scenarios written | `failed`: **23 passed, 1 failed, 1 skipped** (Phytokin's, as expected without it) | `.build/pickle-run-2026-09-23-full-en/` | All twelve scenarios of `03-the-rest-of-testing-md.feature` **passed on their first run**, and so did the rest, but the ultrafast halo: 147 of 300 samples alive. The samples are about 30 game ticks apart at ultrafast (one wait per frame), so 300 samples cover some 8500 ticks and the listener stood up after about 3700: the ages 20 to 450 at the end are a colonist who left, not a dead halo. Before that, stretches of age 1 with the halo destroyed: the job's `tickIntervalAction` did not ping on the last tick of a batch, and the mote is destroyed one tick after its last maintenance |

Fix under test, not run yet: `HaloKeeper`, a `MapComponent` that maintains the halo of every tree pinged in the last 16
ticks, so the halo no longer depends on the listener's ping cadence; and the tick-by-tick step spans game ticks instead of
samples (1500 ticks at ultrafast).

## 2026-09-24

| Time | Run | Result | Evidence folder | What it showed |
| --- | --- | --- | --- | --- |
| 00:0x | halo, first `HaloKeeper` (`Maintain()` in the map's tick), filter `the halo holds` | **no scenario played**, `infrastructure-error` | none | The filter needs the `::` form; the runner gave up before playing. Not a result |
| 00:xx | halo, first `HaloKeeper`, filter `::the halo holds` | `failed`: 1 passed (3x), 2 failed | `.build/pickle-run-2026-09-24-halo-keeper/` | **No better than without it**: normal speed 114 of 151 samples alive, ultrafast 40 of 67 (age 1 and destroyed on the rest), 3x 50 of 50. A `Maintain()` from the map's tick is stamped after the mote's own tick, and `Mote.TimeInterval` destroys on `TicksGame > lastMaintainTick` with no slack (read in the game's IL). The keeper now stamps `lastMaintainTick` one tick ahead |
