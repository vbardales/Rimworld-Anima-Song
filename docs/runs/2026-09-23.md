# Runs of 2026-09-23 and 09-24

Same rig as [2026-09-21.md](2026-09-21.md). Evidence under `.build/` (ignored), minified to text. Times are the
report's file time, read at the copy.

| Time | Run | Result | Evidence folder | What it showed |
| --- | --- | --- | --- | --- |
| 09-23 21:08 | halo only, against the `Maintain()` fix (`b8d7f25`), 3 scenarios | `failed`: 2 passed, 1 failed | pruned 2026-09-24, superseded (its summary was retyped: the shared folder had been overwritten before the copy) | Normal speed and 3x: alive on 300 of 300 ticks, ping age 0 on every one. **The fix works there.** Ultrafast: alive on 186 of 300 samples |
| 09-23 21:21 | full suite, pass A English, 25 scenarios | `failed`: **23 passed, 1 failed, 1 skipped** (Phytokin's, as expected without it) | `.build/pickle-run-2026-09-23-full-en/` (summary and junit) | All twelve scenarios of `03-the-rest-of-testing-md.feature` **passed on their first run**, and so did the rest, but the ultrafast halo: 147 of 300 samples alive. The samples are about 30 game ticks apart at ultrafast (one wait per frame), so 300 samples cover some 8500 ticks and the listener stood up after about 3700: the ages 20 to 450 at the end are a colonist who left, not a dead halo. Before that, stretches of age 1 with the halo destroyed: the job's `tickIntervalAction` did not ping on some ticks and the mote is destroyed on the first tick after its last maintenance |
| 09-24 00:0x | halo, first `HaloKeeper` (`Maintain()` from the map's tick), filter `the halo holds` | **no scenario played**, `infrastructure-error` | none | The filter needs the `::` form; the game loaded no save and the runner gave up. Not a result |
| 09-24 00:xx | halo, first `HaloKeeper`, filter `::the halo holds` | `failed`: 1 passed (3x), 2 failed | pruned 2026-09-24, superseded | **No better than without it**: normal speed 114 of 151 samples alive, ultrafast 40 of 67 (age 1 and destroyed on the rest), 3x 50 of 50. A `Maintain()` from the map's tick is stamped after the mote's own tick, and `Mote.TimeInterval` destroys on `TicksGame > lastMaintainTick` with no slack (game IL) |
| 09-24 07:44 | halo, `HaloKeeper` stamping `lastMaintainTick` one tick ahead (`c04edcc`), 3 scenarios | **`passed`: 3 of 3** | `.build/pickle-run-2026-09-24-halo-ahead/` | Normal speed 150 of 150 samples alive over 300 game ticks, 3x 50 of 50, **ultrafast 52 of 52 over 1518 game ticks** (ping age 1 on 21 of them, alive all the same: the keeper covers the ticks the listener skips) |

## What stands, and what does not

- **Measured, and now fixed:** the halo was alive only on the tick of a ping. Two causes, in the game's own IL
  (`Mote.TimeInterval`): a `needsMaintenance` mote is destroyed on any tick after its last maintained one, with no slack,
  and `lastMaintainTick` starts at 0 (fresh mote never maintained: `b8d7f25`); and the listener's `tickIntervalAction`
  does not ping on every tick at higher speeds (`HaloKeeper`, `c04edcc`).
- **The halo holds in the game's state at 1x, 3x and 15x, on the tree in view**, on the build of `c04edcc`. The rest of the
  suite last ran on the build before `HaloKeeper`; the steps and the mod's other code are unchanged since.
- **Not shown:** that the halo is drawn (the software renderer may not draw the distortion shader), that a sound is heard.
- **Still queued:** the full suite in French and with Phytokin (tickets 14228 and 29164), which stage the current tree.
