# Runs of 2026-09-25 (the song's sound)

Same rig as [2026-09-21.md](2026-09-21.md). Evidence under `Tests/Pickle/Evidence/` (ignored by git), minified to text.
Requests filed through TicketDispatcher, one scenario each; the SHA is in each request's label.

| Time | Run | Result | Evidence folder | What it showed |
| --- | --- | --- | --- | --- |
| 16:23 | `0180`, "the game plays the tree's song when the first listener sits" (my own step reading the game's playing one-shots), bare pass | `failed`, 1 of 1 | `.build/pickle-run-2026-09-25-song-played/` (summary, junit, log) | The order was refused: "no free spot around the tree". The scenario never reached the sound |
| 17:15 | `cbe0`, the same, second try | `failed`, 1 of 1 | `Tests/Pickle/Evidence/pickle-run-2026-09-25-song-played-2/` | Same message: reproducible, not a chance of the game |
| 17:17 | `5c24`, the same, with the seat diagnostics (`1315eaa`) | `failed`, 1 of 1 | `Tests/Pickle/Evidence/pickle-run-2026-09-25-song-played-3/` | The colonist stood at (94, 197), the tree at (70, 132): of the ring's 72 cells none forbidden, none unstandable, none reserved, none out of sight, **all 72 unreachable, 0 free**. The fixture spawns a new colonist where the scenario does not choose, and this one was cut off from the tree. A fault of the scenario, not of the mod |
| 21:44 | `3bf5`, "the game plays Royalty's anima linking sound when the first listener sits", **sound pass** (`wsl-deps.sound.map`, PickleTools' SoundCapture), colonist placed beside the tree (`b3404b4`) | **`passed`**, 1 of 1, 13 s | `Tests/Pickle/Evidence/pickle-run-2026-09-25-sound-royalty/` | The game itself holds `AnimaTreeLink` among its playing sounds when the first listener sits. **This is the green replay of the red above**: the same check, rewritten with PickleTools' step `the game is playing the sound {string}` instead of mine and with the colonist stood near the tree. It does not say that the audio output renders it |
