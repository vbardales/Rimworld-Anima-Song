# Runs of 2026-09-26 (the save check, the waves, the French inspect line)

Same rig as [2026-09-21.md](2026-09-21.md). Requests filed through TicketDispatcher; the SHA is in each request's label.
Green rows are added when the RUN_DONE messages of `33c5` and `f267` arrive.

| Run | Scenario | Result | What it showed |
| --- | --- | --- | --- |
| `1353` | what a save holds of the mod | `failed` | The save names the class `AnimaSong.HaloKeeper`: the game writes the name of every map component, this one holds no data. A defect of my allow-list, not of the mod; fixed in `3e05e00` |
| `3eca` | the tree's interface, waves | `failed` | 0 motes found in `map.listerThings`: motes are drawn from `DynamicDrawManager.drawThings`. A defect of the step; fixed in `3e05e00` |
| `4ac7`, `6079` | the same two | `failed`, same messages | They ran with the committed steps DLL of before the fix. Rebuilt and committed in `ca3941a` |
| `33c5` | what a save holds of the mod | pending | on `ca3941a` |
| `f267` | the tree's interface, French | pending | on `ca3941a` |
